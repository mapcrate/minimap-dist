{
  "version": "0.2.9",
  "url": "https://github.com/mapcrate/minimap-dist/releases/download/v0.2.9/minimap.exe",
  "notes": "Smoother panning on weak devices (tile loader: capped decode concurrency + coalesced repaints). New plugin market — pick a quality variant at install."
}
